# Ambiente virtual em Python (venv)
# Um ambiente virtual carrega toda a sua instalacao
# do Python para um pasta no caminho escolido
# Ao ativar um venv, a instalacao do venv sera usada
# venv pe o modulo que usamos para cria ambiente virtuais
# voce pode dar o nome que preferir para um venv, mais o mais comum é:
# venv env  .venv .env

# lembrar:  
#   python -m venv "qualquer nome aqui mais por custume" venv e sem ""
#   python -m venv venv

#   .\venv\scripts\activate isso para ativar o venv
#   deactivate isso para desativar
